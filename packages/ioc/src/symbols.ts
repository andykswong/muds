/** Module metadata key */
export const MODULE = Symbol('module');

/** Provider metadata key */
export const PROVIDER = Symbol('provider');

/** Id tag key */
export const TAG_ID = Symbol('id');

/** Multi-inject tag key */
export const TAG_MULTI = Symbol('multi');

/** Singleton tag key */
export const TAG_SINGLETON = Symbol('singleton');